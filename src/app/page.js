import Coin from "@/../../components/Clicker/coin";
import React, { Suspense } from "react";

export default function Home() {
    return (
        <Suspense>
            <Coin/>
        </Suspense>
    );
}
